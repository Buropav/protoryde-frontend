{
  "buildCommand": "npx expo export -p web && node scripts/postexport.js",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}